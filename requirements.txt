# Terra Guide — requirements.txt
# Install with:  pip install -r requirements.txt

# Core UI
pygame>=2.5.0

# Image processing
Pillow>=10.0.0

# TTS — Microsoft Neural (100% human English voice, no API key needed)
edge-tts>=6.1.9

# TTS — Google fallback (online, clear English)
gTTS>=2.5.0

# TTS — offline fallback
pyttsx3>=2.90

# Speech Recognition (STT)
SpeechRecognition>=3.10.0

# Arduino communication
pyserial>=3.5

# AI Chatbot
openai>=1.30.0

# Web dashboard
flask>=3.0.0

# HTTP requests (laptop_face.py → Pi polling)
requests>=2.31.0

# Sensor math (optional but useful)
numpy>=1.24.0

# python-dotenv (lexo .env)
python-dotenv>=1.0.0

# Note for Raspberry Pi:
# sudo apt install python3-pygame portaudio19-dev espeak
# pip install RPi.GPIO          ← vetëm në Raspberry Pi
# pip install --break-system-packages -r requirements.txt